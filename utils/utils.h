#ifndef UTILS_H
#define UTILS_H

int simple_search(int target, int* arr, int arr_size);
int fast_search(int target, int* arr, int arr_size); /* Only works with sorted arrays... */
void sort(int* arr, int arr_size);

#endif